#pragma once
#include <Arduino.h>

// Central configuration for client
// Fill these in for your deployment

static const String BASE_URL = "https://your-server.example.com"; // no trailing slash
static const String DEVICE_KEY = "demo-key";